[user]
	email = koki.111.sui@gmail.com
	name = one1color
[alias]
	aa = add -A
	s = status
	c = commit
	cm = commit -m
	pom = push origin main
	p = push
[core]
	excludesfile = /Users/kokiisshiki/.gitignore
	editor = /usr/bin/vim
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
